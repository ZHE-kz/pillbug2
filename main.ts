import { serve } from './deps.ts'; import { loadQuestions } from './sheets.ts'; import { handleLineRequest } from './line.ts'; await loadQuestions(); console.log('🦕 LINE Bot Server running on http://localhost:8000/'); serve(async (req) => { const url = new URL(req.url); if(req.method==='POST' && url.pathname==='/webhook'){ return await handleLineRequest(req); } return new Response('🐭 LINE Bot Running!',{status:200}); });
