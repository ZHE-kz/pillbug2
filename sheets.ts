let QUESTIONS:any[]=[]; const SHEET_ID='1izdJjnA9XvlCr1kp9Izc9tUNFnoKUBIscZFZk3styus'; const GOOGLE_API_KEY='YOUR_GOOGLE_API_KEY'; export async function loadQuestions(){ const url=https://sheets.googleapis.com/v4/spreadsheets//values/Questions?key=; const res=await fetch(url); const data=await res.json(); const rows=data.values||[]; QUESTIONS=rows.slice(1).map((row,i)=>({globalIndex:i,story:row[0],question:row[1],options:[row[2],row[3],row[4],row[5]],answer:Number(row[6]),explain:row[7],chapter:row[8]||0,qnum:i+1})); console.log(✅ Loaded  questions); } export function getQuestions(){ return QUESTIONS; }
